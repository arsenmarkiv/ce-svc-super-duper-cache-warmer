//IMPORTANT: Do NOT remove the underscore (it's magic)
//This is a jenkins shared library to run the pipeline
@Library("eagle-jenkins-pipeline") _

/*
 * IMPORTANT: You must fill out these values when setting up your service.
 */
pipeline_ServicePython {
    projectName      = "svc-dnahelloworld"
    repoName         = "ce-svc-dnahelloworld"
    slackChannel     = "#alert-devops"
    teamEmail        = "innolab@eagleinvsys.com"
    ignoreXrayAlerts = true
    pythonVersion    = "3.8"
    useHarness       = true
}
